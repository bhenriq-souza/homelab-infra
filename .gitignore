**/.terraform/*

# Terraform state — gerenciado no GCS (homelab-492918-tf-state), nunca commitar
**/*.tfstate
**/*.tfstate.*
**/tfplan

# Local sensitive credentials (never commit real keys)
secrets/*/*
!secrets/README.md
!secrets/*/.gitkeep
!secrets/*/gcp-sa.json.example