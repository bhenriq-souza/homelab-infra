apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: dev-apps
  labels:
    app.kubernetes.io/name: myapp
    homelab.io/environment: dev
spec:
  selector:
    app.kubernetes.io/name: myapp
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
